const API='https://abnydnnafyosrakkgajy.supabase.co/functions/v1/v6-test1-api';let user=null,home=null;const $=x=>document.getElementById(x);async function api(a,b={}){const r=await fetch(`${API}?a=${a}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)}),t=await r.text();let d;try{d=JSON.parse(t)}catch{d=t}if(!r.ok)throw Error(d?.error||t);return d}async function login(){try{const d=await api('login',{pin:$('pin').value.trim()});user=Array.isArray(d)?d[0]:d;if(!user)throw Error('PIN not recognized.');user.employee_id=user.employee_id||user.id;$('login').classList.add('hidden');$('app').classList.remove('hidden');await refresh()}catch(e){$('loginMsg').textContent=e.message}}async function refresh(){try{home=await api('home',{employee_id:user.employee_id});const e=home.employee;$('hello').textContent=`Hello, ${e.name}`;$('role').textContent=e.role||e.access_level||'';const tasks=home.tasks||[];$('badge').textContent=tasks.length;$('tasks').innerHTML=tasks.length?tasks.map((t,i)=>`<button class="task" data-i="${i}"><span><b>${esc(t.title)}</b><small>${esc(t.detail||'')}</small></span><strong>›</strong></button>`).join(''):'<div class="empty">✓ You have no open assignments or notifications.</div>';$('management').classList.toggle('hidden',!home.permissions.management);bindTasks()}catch(e){$('homeMsg').textContent=e.message}}function bindTasks(){document.querySelectorAll('.task').forEach(b=>b.onclick=()=>openTask(home.tasks[+b.dataset.i]))}function openTask(t){if(t.type==='verification'){location.href=`manager.html?mode=verify&ca=${encodeURIComponent(t.id)}&pin=${encodeURIComponent($('pin').value)}`;return}if(t.type==='corrective_action'){location.href=`manager.html?finding=${encodeURIComponent(t.finding_id)}&pin=${encodeURIComponent($('pin').value)}`;return}show(`${t.title}\n\n${t.detail||''}\n\nStatus: ${t.status||'open'}`)}function module(name){const live={inspection:'inspection.html',management:'manager.html'};if(live[name]){location.href=live[name]+`?pin=${encodeURIComponent($('pin').value)}`;return}show(`${name.replaceAll('_',' ').toUpperCase()}\n\nThis module is now reserved on the unified EIM home screen. We will connect its existing workflow next without changing your login.`)}function show(txt){$('modalText').textContent=txt;$('modal').classList.remove('hidden')}function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}$('go').onclick=login;$('refresh').onclick=refresh;$('close').onclick=()=>$('modal').classList.add('hidden');document.querySelectorAll('[data-module]').forEach(b=>b.onclick=()=>module(b.dataset.module));$('logout').onclick=()=>location.reload();